{
  "name": "grab-hackathon-2026",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=web",
    "dev:api": "cd apps/api && uv run uvicorn app.main:app --reload --host 127.0.0.1 --port 8000",
    "build:web": "npm run build --workspace=web"
  }
}
